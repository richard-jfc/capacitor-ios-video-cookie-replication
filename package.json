{
  "name": "capacitor-ios-video-cookie-replication",
  "version": "1.0.0",
  "description": "Minimal reproduction: CapacitorCookies' prompt()-based document.cookie bridge pauses <video> playback on iOS",
  "private": true,
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "7.6.8",
    "@capacitor/ios": "7.6.8"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.8"
  }
}
